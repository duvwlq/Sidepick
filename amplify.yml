version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd fe
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: fe/dist
    files:
      - '**/*'
  cache:
    paths:
      - fe/node_modules/**/*
