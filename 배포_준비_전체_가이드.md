# Sidepick 배포/운영 가이드

이 문서는 현재 실제로 적용된 MVP 배포 구조를 기준으로 정리한 운영 가이드입니다.

## 현재 배포 구조

- 프론트엔드: AWS Amplify
- 백엔드: AWS EC2 + Nginx + Spring Boot
- DB: AWS RDS MySQL
- 프론트 URL:
  - `https://codex-backend-mvp-verify.d1kbzcbfbyz1zc.amplifyapp.com`
- 백엔드 URL:
  - `https://api.side-pick.app/api`

## 1. 운영 중 꼭 유지할 값

EC2의 `~/backend.env`에서 최소한 아래 값은 정확해야 합니다.

```env
SPRING_PROFILES_ACTIVE='prod'
SERVER_PORT='8081'
SPRING_JPA_HIBERNATE_DDL_AUTO='validate'
APP_CORS_ALLOWED_ORIGINS='https://codex-backend-mvp-verify.d1kbzcbfbyz1zc.amplifyapp.com'
APP_JWT_SECRET='<LONG-RANDOM-SECRET>'
```

주의:

- JDBC URL은 `&` 때문에 반드시 따옴표로 감싸야 함
- `ddl-auto`는 운영에서 `validate` 유지
- CORS는 실제 프론트 도메인만 허용

## 2. 백엔드 실행/재시작

```bash
pkill -f failforward-backend || true
set -a
source ~/backend.env
set +a
cd ~/Sidepick/server
nohup java -jar target/failforward-backend-0.0.1-SNAPSHOT.jar > ~/backend.log 2>&1 &
sleep 10
tail -n 80 ~/backend.log
```

정상 확인:

```bash
curl https://api.side-pick.app/api/health
```

## 3. Nginx / HTTPS

현재 공개 트래픽은 `api.side-pick.app`로 받고, Nginx가 내부 `8081`로 프록시합니다.

인증서는 Certbot으로 발급:

```bash
sudo certbot --nginx -d api.side-pick.app
```

확인:

```bash
curl https://api.side-pick.app/api/health
```

## 4. RDS 비밀번호 교체 가이드

권장 상황:

- 비밀번호가 채팅, 터미널 기록, 스크린샷에 노출된 경우
- 팀원 공유용 서버로 전환하는 경우

교체 순서:

1. AWS 콘솔
2. RDS
3. `sidepick-db`
4. `Modify`
5. `Credentials settings`
6. 새 비밀번호 입력
7. `Apply immediately`
8. `Modify DB instance`

그 다음 EC2에서 `~/backend.env` 수정:

```env
SPRING_DATASOURCE_PASSWORD='<NEW-RDS-PASSWORD>'
```

재시작:

```bash
pkill -f failforward-backend || true
set -a
source ~/backend.env
set +a
cd ~/Sidepick/server
nohup java -jar target/failforward-backend-0.0.1-SNAPSHOT.jar > ~/backend.log 2>&1 &
```

검증:

```bash
curl https://api.side-pick.app/api/health
```

## 5. Amplify 커스텀 도메인 연결

현재는 Amplify 기본 도메인을 사용 중입니다. 원하면 `side-pick.app` 또는 `www.side-pick.app`를 프론트에 연결할 수 있습니다.

권장 구조:

- 프론트: `https://side-pick.app`
- 백엔드: `https://api.side-pick.app`

Amplify 연결 순서:

1. Amplify 앱 진입
2. `호스팅`
3. `사용자 지정 도메인`
4. `도메인 추가`
5. `side-pick.app` 입력
6. 서브도메인 매핑 선택

권장 매핑:

- `@` -> Amplify 앱
- `www` -> Amplify 앱 또는 `@`로 리다이렉트

연결 후 프론트 환경변수는 그대로 유지 가능:

```env
VITE_API_BASE_URL=https://api.side-pick.app/api
```

다만 프론트 도메인이 바뀌면 CORS는 백엔드에 추가해야 합니다.

예:

```env
APP_CORS_ALLOWED_ORIGINS='https://codex-backend-mvp-verify.d1kbzcbfbyz1zc.amplifyapp.com,https://side-pick.app,https://www.side-pick.app'
```

백엔드 재시작 필요:

```bash
pkill -f failforward-backend || true
set -a
source ~/backend.env
set +a
cd ~/Sidepick/server
nohup java -jar target/failforward-backend-0.0.1-SNAPSHOT.jar > ~/backend.log 2>&1 &
```

## 6. 보안 체크리스트

- EC2 보안그룹에서 `8081` 외부 인바운드 제거
- `22`는 내 IP만 허용
- `80`, `443`만 공개
- JWT secret 운영용 값 사용
- RDS 비밀번호 정기 교체
- CORS는 실제 프론트 도메인만 허용

## 7. 점검 명령어

백엔드 상태:

```bash
curl https://api.side-pick.app/api/health
```

Nginx 설정 검사:

```bash
sudo nginx -t
```

백엔드 로그:

```bash
tail -n 100 ~/backend.log
```

인증서 만료 확인:

```bash
sudo certbot certificates
```

## 8. 현재 완료 상태

- EC2 배포 완료
- RDS 연결 완료
- HTTPS 완료
- Amplify 배포 완료
- Mixed Content 해결 완료
- CORS 해결 완료
- 프론트에서 실제 데이터 조회 확인 완료
