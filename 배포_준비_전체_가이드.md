# Sidepick 배포/운영 가이드

이 문서는 현재 실제 운영 구조를 기준으로 정리한 운영 가이드입니다.

## 현재 배포 구조

- 프론트엔드: AWS Amplify
- 백엔드: AWS EC2 + Docker Compose
- 리버스 프록시: Nginx container
- AI 서버: FastAPI container
- DB: AWS RDS MySQL
- 프론트 URL:
  - `https://side-pick.app`
  - `https://www.side-pick.app`
- 백엔드 URL:
  - `https://api.side-pick.app/api`

## 1. 운영 중 꼭 유지할 값

운영 기준점은 EC2의 `/home/ubuntu/sidepick-docker/.env`입니다.

```env
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8081
SPRING_JPA_HIBERNATE_DDL_AUTO=validate
APP_CORS_ALLOWED_ORIGINS=https://side-pick.app,https://www.side-pick.app
APP_JWT_SECRET=<LONG-RANDOM-SECRET>
AI_SERVER_URL=http://ai-server:8001
APP_AUTH_LOCAL_ENABLED=false
APP_AUTH_KAKAO_ENABLED=true
APP_AUTH_GOOGLE_ENABLED=false
APP_MAIL_ENABLED=false
APP_DEMO_SEED_ENABLED=false
```

주의:

- 운영에서 `APP_DEMO_SEED_ENABLED`는 반드시 `false`
- `ddl-auto`는 운영에서 `validate`
- `AI_SERVER_URL`은 운영 Compose 네트워크 기준 `http://ai-server:8001`

## 2. 백엔드 재배포

```bash
cd /home/ubuntu/sidepick-docker/infra
sudo docker compose --env-file ../.env -f docker-compose.prod.yml up -d --build backend
```

상태 확인:

```bash
cd /home/ubuntu/sidepick-docker/infra
sudo docker compose --env-file ../.env -f docker-compose.prod.yml ps
```

로그 확인:

```bash
cd /home/ubuntu/sidepick-docker/infra
sudo docker compose --env-file ../.env -f docker-compose.prod.yml logs -f backend
```

정상 확인:

```bash
curl http://127.0.0.1:8081/api/health
curl https://api.side-pick.app/api/health
```

## 3. Nginx / HTTPS

현재 공개 트래픽은 `api.side-pick.app`으로 받고, Nginx 컨테이너가 내부 `127.0.0.1:8081`로 프록시합니다.

관련 경로:

- Compose: `/home/ubuntu/sidepick-docker/infra/docker-compose.prod.yml`
- Nginx 설정: `/home/ubuntu/sidepick-docker/infra/nginx/nginx.prod.conf`

## 4. RDS 비밀번호 교체 시

1. RDS에서 비밀번호 변경
2. `/home/ubuntu/sidepick-docker/.env`의 `SPRING_DATASOURCE_PASSWORD` 수정
3. 필요 시 `/home/ubuntu/config/backend.env`, `/home/ubuntu/app/backend.env`도 동기화
4. 백엔드 재배포
5. health check 확인

## 5. 운영 데이터 반영 원칙

- demo/local 성격 데이터는 운영에 넣지 않음
- 경험 CSV를 반영할 때는 연관 분석 테이블 포함 삭제 범위를 먼저 확인
- import 컨테이너는 일회성으로 실행
- import 후 총 건수, 카테고리 분포, health check를 반드시 확인

## 6. 보안 체크리스트

- `22`는 허용 IP만 오픈
- 외부 공개 포트는 `80`, `443` 중심 유지
- JWT secret은 운영용 랜덤값 사용
- RDS 비밀번호 정기 교체
- CORS는 실제 프론트 도메인만 허용

## 7. 빠른 점검 명령어

```bash
cd /home/ubuntu/sidepick-docker/infra
sudo docker compose --env-file ../.env -f docker-compose.prod.yml ps
sudo docker compose --env-file ../.env -f docker-compose.prod.yml logs --tail 100 backend
curl http://127.0.0.1:8081/api/health
curl https://api.side-pick.app/api/health
```
